added this file onto REMOTE-MASTSER directly on GITHUB.com (using this to test out the 'merge master into branch before pushing branches changes to repo' process. This file was added to the MASTER, so 
will try to get this onto my local branch via the git pull command that were supposed to do AFTER committing our local changes and BEFORE we PUSH those changes 
